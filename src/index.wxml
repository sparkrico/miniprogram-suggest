<view class='search-title' style='{{titleStyle}}'>{{titleValue}}</view>
  <view class='search-content'>
    <view class='search-item' style='{{itemStyle}}' wx:for="{{listData}}" wx:key="listData" bindtap="onItemTap" data-keyword="{{item}}">
      {{item}}
    </view>
  </view>
